name: GA2 Action

on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: 23f3001285@ds.study.iitm.ac.in
        run: echo "Email step present"

      - name: prime-cache-6ff164d
        run: echo "Priming cache step"

      - name: Final Step
        run: echo "Done"
